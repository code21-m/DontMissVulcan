<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DontMissVulcan.Views.Recruitment.RecruitmentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DontMissVulcan.Views.Recruitment"
    xmlns:tagselection="using:DontMissVulcan.ViewModels.TagSelection"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ItemsControl ItemsSource="{x:Bind ViewModel.TagSelector.TagCategories}">
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="tagselection:TagCategoryViewModel">
                    <StackPanel>
                        <TextBlock Text="{x:Bind Name}" />

                        <GridView SelectionMode="None" IsItemClickEnabled="False" ItemsSource="{x:Bind TagItems}">
                            <GridView.ItemTemplate>
                                <DataTemplate x:DataType="tagselection:TagItemViewModel">
                                    <ToggleButton Width="120" Height="40"
                                              Content="{x:Bind DisplayName}"
                                              IsChecked="{x:Bind IsSelected, Mode=TwoWay}"
                                              IsEnabled="{x:Bind IsSelectable, Mode=TwoWay}" />
                                </DataTemplate>
                            </GridView.ItemTemplate>
                        </GridView>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
